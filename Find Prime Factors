Q. WAP TO GET THE FACTORS OF A NUMBER IN THE FORM OF A LIST 
SOL.

def getprimef(N):
    factors= list()
    divisor= 2
    while(divisor<=N):
        if (N%divisor) == 0 :
            factors.append(divisor)
            N = N/divisor
        else:
            divisor+=1
    return factors        
    
print(getprimef(630))
